# UI Components

Shared UI components library for RoboHatch Platform.

## Components

- `Button`: A reusable button component with variants and sizes

## Usage

```tsx
import { Button } from '@robohatch/ui'

function App() {
  return (
    <Button variant="primary" size="md">
      Click me
    </Button>
  )
}
```
